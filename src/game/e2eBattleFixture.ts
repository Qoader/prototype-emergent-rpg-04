import type { WorldMap } from './types';
export function createBattleFixture(multi = true, encounterAtSpawn = true): WorldMap { const width=12,height=12; const encounter=encounterAtSpawn?{col:1,row:1}:{col:4,row:4}; return { width,height,spawn:{col:1,row:1},tiles:Array.from({length:width*height},(_,i)=>({col:i%width,row:Math.floor(i/width),kind:'grass' as const,walkable:true,settlementId:i%width<=3&&Math.floor(i/width)<=3?'fixture-settlement':undefined})),settlements:[{id:'fixture-settlement',name:'Fixture Settlement',kind:'village',countryId:'fixture',col:1,row:1,radius:1,bounds:{left:0,top:0,right:3,bottom:3},gates:[]}],goblinNests:[{id:'fixture-nest',col:encounter.col,row:encounter.row,spawnTiles:multi?[encounter,encounter,encounter]:[encounter,{col:10,row:10},{col:10,row:9}]}]}; }
